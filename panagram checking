class Solution:
    def checkPangram(self,s):
        s=s.lower()
        
        letters=set()
        
        for ch in s:
            if'a'<=ch<='z':
                letters.add(ch)
                
        return len(letters)==26
